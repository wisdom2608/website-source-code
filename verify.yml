- name: Checkout Source Repository
  uses: actions/checkout@v4

- name: Checkout Kubernetes Manifests Repository
  uses: actions/checkout@v4
  with:
    repository: wisdom2608/website-k8s-manifests
    path: manifests

# This code didn't work because the checkout action doesn't support checking out multiple repositories in the same step. You need to use separate steps for each repository checkout. 